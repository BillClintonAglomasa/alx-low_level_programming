This project (0x06 - More pointers, arrays and strings) concentrates on pointers,
arrays and strings  in C programming languag.

1. 0-strcat.c
This is a C function that concatenates two strings.

2. 1-strncat.c
This is a C function that concatenates two strings that uses n bytes.

3. 2-strncpy.c
This is a C function that copies a string.

4. 3-strcmp.c
This is a C function that compares two strings.

5. 4-rev_array.c
This is a C function that reverses the content of an array of integers.

6. 5-string_toupper.c
A C function that changes all lowercase letters of a string to uppercase.

7. 6-cap_string.c
A C function that capitalizes all words of a string.

8. 7-leet.c
A C function that encodes a string into 1337.

9. 100-rot13.c
A C function that encodes a string using rot13.

10. 101-print_number.c
A C function that prints an integer.

11. 102-magic.c
A C code in which I add a line of code so that the progrma prints a[2] = 98,
followed by a newline.

12. 103-infinite_add.c
A C function that adds two numbers.

13. 104-print_buffer.c
A C function that prints a buffer.
