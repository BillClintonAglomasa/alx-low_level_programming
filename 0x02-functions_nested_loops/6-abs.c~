#include "main.h"
#include <stdio.h>

/**
 * abs - computes the absolute value of an integer
 *
 * @int: The int to check
 *
 * Return: Always 0
 */
int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
